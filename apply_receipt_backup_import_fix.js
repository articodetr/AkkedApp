const fs = require('fs');
const path = require('path');

const root = process.cwd();
const receiptPath = path.join(root, 'app', 'receipt-preview.tsx');
const backupPath = path.join(root, 'app', 'backup.tsx');
const report = [];

function ensureFile(filePath) {
  if (!fs.existsSync(filePath)) {
    throw new Error(`File not found: ${filePath}`);
  }
}

function writeBackupFile() {
  ensureFile(backupPath);
  const nextBackup = "import { useState } from 'react';\nimport {\n  View,\n  Text,\n  StyleSheet,\n  TouchableOpacity,\n  ScrollView,\n  Alert,\n  ActivityIndicator,\n} from 'react-native';\nimport { useRouter } from 'expo-router';\nimport { ArrowRight, Download, Upload, Database, Calendar } from 'lucide-react-native';\nimport * as FileSystem from 'expo-file-system/legacy';\nimport * as Sharing from 'expo-sharing';\nimport * as DocumentPicker from 'expo-document-picker';\nimport { supabase } from '@/lib/supabase';\nimport { useAuth } from '@/contexts/AuthContext';\nimport { fetchAccessibleCustomerIds } from '@/services/userScopeService';\n\ntype BackupTableKey =\n  | 'customers'\n  | 'transactions'\n  | 'debts'\n  | 'account_movements'\n  | 'exchange_rates';\n\ntype BackupPayload = {\n  version?: string;\n  timestamp?: string;\n  generated_by?: Record<string, unknown>;\n  data?: Partial<Record<BackupTableKey, any[]>>;\n};\n\nfunction asArray(payload: BackupPayload, key: BackupTableKey): any[] {\n  const value = payload?.data?.[key];\n  return Array.isArray(value) ? value : [];\n}\n\nfunction cleanRow(row: any) {\n  const cleaned = { ...(row || {}) };\n  delete cleaned.linked_user;\n  delete cleaned.customers;\n  delete cleaned.customer;\n  delete cleaned.commission_recipient;\n  return cleaned;\n}\n\nasync function upsertRows(\n  tableName: BackupTableKey,\n  rows: any[],\n  transform?: (row: any) => any,\n) {\n  const preparedRows = rows.map((row) => {\n    const cleaned = cleanRow(row);\n    return transform ? transform(cleaned) : cleaned;\n  });\n\n  if (preparedRows.length === 0) {\n    return 0;\n  }\n\n  const chunkSize = 250;\n  for (let i = 0; i < preparedRows.length; i += chunkSize) {\n    const chunk = preparedRows.slice(i, i + chunkSize);\n    const { error } = await supabase.from(tableName).upsert(chunk, {\n      onConflict: 'id',\n    });\n\n    if (error) {\n      throw error;\n    }\n  }\n\n  return preparedRows.length;\n}\n\nexport default function BackupScreen() {\n  const router = useRouter();\n  const { currentUser } = useAuth();\n  const [isExporting, setIsExporting] = useState(false);\n  const [isImporting, setIsImporting] = useState(false);\n  const [lastBackup, setLastBackup] = useState<string | null>(null);\n\n  const handleExportData = async () => {\n    setIsExporting(true);\n\n    try {\n      if (!currentUser?.userId) {\n        Alert.alert('خطأ', 'يجب تسجيل الدخول أولاً');\n        return;\n      }\n\n      const accessibleCustomerIds = await fetchAccessibleCustomerIds(currentUser.userId, true);\n\n      const [customers, transactions, debts, movements, exchangeRates] = await Promise.all([\n        accessibleCustomerIds.length > 0\n          ? supabase.from('customers').select('*').in('id', accessibleCustomerIds)\n          : Promise.resolve({ data: [], error: null } as any),\n        accessibleCustomerIds.length > 0\n          ? supabase.from('transactions').select('*').in('customer_id', accessibleCustomerIds)\n          : Promise.resolve({ data: [], error: null } as any),\n        accessibleCustomerIds.length > 0\n          ? supabase.from('debts').select('*').in('customer_id', accessibleCustomerIds)\n          : Promise.resolve({ data: [], error: null } as any),\n        accessibleCustomerIds.length > 0\n          ? supabase.from('account_movements').select('*').in('customer_id', accessibleCustomerIds)\n          : Promise.resolve({ data: [], error: null } as any),\n        supabase.from('exchange_rates').select('*'),\n      ]);\n\n      const firstError = [\n        customers.error,\n        transactions.error,\n        debts.error,\n        movements.error,\n        exchangeRates.error,\n      ].find(Boolean);\n\n      if (firstError) {\n        throw firstError;\n      }\n\n      const backupData: BackupPayload = {\n        version: '2.0',\n        timestamp: new Date().toISOString(),\n        generated_by: {\n          user_id: currentUser.userId,\n          user_name: currentUser.userName,\n          account_number: currentUser.accountNumber,\n        },\n        data: {\n          customers: customers.data || [],\n          transactions: transactions.data || [],\n          debts: debts.data || [],\n          account_movements: movements.data || [],\n          exchange_rates: exchangeRates.data || [],\n        },\n      };\n\n      if (!FileSystem.documentDirectory) {\n        throw new Error('تعذر الوصول إلى مساحة التخزين المحلية');\n      }\n\n      const timestamp = new Date().toISOString().replace(/[:.]/g, '-');\n      const fileName = `backup_${currentUser.userName || 'user'}_${timestamp}.json`;\n      const fileUri = `${FileSystem.documentDirectory}${fileName}`;\n\n      await FileSystem.writeAsStringAsync(fileUri, JSON.stringify(backupData, null, 2), {\n        encoding: FileSystem.EncodingType.UTF8,\n      });\n\n      const canShare = await Sharing.isAvailableAsync();\n      if (canShare) {\n        await Sharing.shareAsync(fileUri, {\n          mimeType: 'application/json',\n          dialogTitle: 'مشاركة النسخة الاحتياطية',\n          UTI: 'public.json',\n        });\n      }\n\n      Alert.alert(\n        'نجح',\n        canShare\n          ? 'تم إنشاء النسخة الاحتياطية ومشاركتها بنجاح.'\n          : `تم إنشاء النسخة الاحتياطية بنجاح.\\n\\nمسار الملف:\\n${fileUri}`,\n        [{ text: 'حسنًا' }],\n      );\n\n      setLastBackup(new Date().toISOString());\n      console.log('Backup data created:', fileUri);\n    } catch (error) {\n      console.error('Error exporting data:', error);\n      Alert.alert('خطأ', 'حدث خطأ أثناء تصدير البيانات');\n    } finally {\n      setIsExporting(false);\n    }\n  };\n\n  const importBackupFile = async () => {\n    if (!currentUser?.userId) {\n      Alert.alert('خطأ', 'يجب تسجيل الدخول أولاً');\n      return;\n    }\n\n    setIsImporting(true);\n\n    try {\n      const pickerResult = await DocumentPicker.getDocumentAsync({\n        type: 'application/json',\n        copyToCacheDirectory: true,\n        multiple: false,\n      });\n\n      if (pickerResult.canceled) {\n        return;\n      }\n\n      const selectedFile = pickerResult.assets?.[0];\n      if (!selectedFile?.uri) {\n        Alert.alert('خطأ', 'لم يتم اختيار ملف صالح');\n        return;\n      }\n\n      const fileContent = await FileSystem.readAsStringAsync(selectedFile.uri, {\n        encoding: FileSystem.EncodingType.UTF8,\n      });\n\n      const backupData = JSON.parse(fileContent) as BackupPayload;\n\n      if (!backupData?.data || typeof backupData.data !== 'object') {\n        Alert.alert('خطأ', 'ملف النسخة الاحتياطية غير صالح');\n        return;\n      }\n\n      const customers = asArray(backupData, 'customers');\n      const transactions = asArray(backupData, 'transactions');\n      const debts = asArray(backupData, 'debts');\n      const movements = asArray(backupData, 'account_movements');\n      const exchangeRates = asArray(backupData, 'exchange_rates');\n\n      const importedCustomers = await upsertRows('customers', customers, (row) => ({\n        ...row,\n        user_id: currentUser.userId,\n      }));\n\n      const importedExchangeRates = await upsertRows('exchange_rates', exchangeRates);\n      const importedMovements = await upsertRows('account_movements', movements);\n      const importedTransactions = await upsertRows('transactions', transactions);\n      const importedDebts = await upsertRows('debts', debts);\n\n      Alert.alert(\n        'تم الاستيراد',\n        `تم استيراد البيانات بنجاح:\\n\\n` +\n          `• العملاء: ${importedCustomers}\\n` +\n          `• الحركات: ${importedMovements}\\n` +\n          `• الحوالات: ${importedTransactions}\\n` +\n          `• الديون: ${importedDebts}\\n` +\n          `• أسعار الصرف: ${importedExchangeRates}`,\n        [{ text: 'حسنًا' }],\n      );\n    } catch (error: any) {\n      console.error('Error importing data:', error);\n      Alert.alert(\n        'خطأ',\n        error?.message\n          ? `حدث خطأ أثناء استيراد البيانات:\\n${error.message}`\n          : 'حدث خطأ أثناء استيراد البيانات',\n      );\n    } finally {\n      setIsImporting(false);\n    }\n  };\n\n  const handleImportData = () => {\n    Alert.alert(\n      'استيراد البيانات',\n      'سيتم اختيار ملف JSON واستيراد البيانات منه. إذا كانت بعض البيانات موجودة مسبقًا سيتم تحديثها بدل تكرارها. هل تريد المتابعة؟',\n      [\n        { text: 'إلغاء', style: 'cancel' },\n        { text: 'اختيار ملف', onPress: importBackupFile },\n      ],\n    );\n  };\n\n  return (\n    <View style={styles.container}>\n      <View style={styles.header}>\n        <TouchableOpacity style={styles.backButton} onPress={() => router.back()}>\n          <ArrowRight size={24} color=\"#111827\" />\n        </TouchableOpacity>\n        <Text style={styles.headerTitle}>النسخ الاحتياطي</Text>\n        <View style={{ width: 40 }} />\n      </View>\n\n      <ScrollView style={styles.content}>\n        <View style={styles.infoCard}>\n          <Database size={48} color=\"#4F46E5\" />\n          <Text style={styles.infoTitle}>النسخ الاحتياطي للبيانات</Text>\n          <Text style={styles.infoText}>\n            يمكنك الآن تصدير نسخة JSON فعلية من بياناتك أو استيراد نسخة محفوظة سابقًا.\n            يتم تضمين واستعادة البيانات التابعة للحساب الحالي فقط.\n          </Text>\n\n          {lastBackup && (\n            <View style={styles.lastBackupContainer}>\n              <Calendar size={16} color=\"#6B7280\" />\n              <Text style={styles.lastBackupText}>\n                آخر نسخة احتياطية: {new Date(lastBackup).toLocaleDateString('ar-EG')}\n              </Text>\n            </View>\n          )}\n        </View>\n\n        <View style={styles.actionsSection}>\n          <TouchableOpacity\n            style={[styles.actionCard, { backgroundColor: '#EEF2FF' }]}\n            onPress={handleExportData}\n            disabled={isExporting || isImporting}\n          >\n            {isExporting ? (\n              <ActivityIndicator color=\"#4F46E5\" />\n            ) : (\n              <Download size={40} color=\"#4F46E5\" />\n            )}\n            <Text style={styles.actionTitle}>تصدير البيانات</Text>\n            <Text style={styles.actionDescription}>\n              {isExporting ? 'جاري التصدير...' : 'تصدير ومشاركة نسخة JSON'}\n            </Text>\n          </TouchableOpacity>\n\n          <TouchableOpacity\n            style={[styles.actionCard, { backgroundColor: '#ECFDF5' }]}\n            onPress={handleImportData}\n            disabled={isExporting || isImporting}\n          >\n            {isImporting ? (\n              <ActivityIndicator color=\"#10B981\" />\n            ) : (\n              <Upload size={40} color=\"#10B981\" />\n            )}\n            <Text style={styles.actionTitle}>استيراد البيانات</Text>\n            <Text style={styles.actionDescription}>\n              {isImporting ? 'جاري الاستيراد...' : 'استعادة البيانات من نسخة احتياطية'}\n            </Text>\n          </TouchableOpacity>\n        </View>\n\n        <View style={styles.warningCard}>\n          <Text style={styles.warningTitle}>تنبيه هام</Text>\n          <Text style={styles.warningText}>\n            • احتفظ بنسخ احتياطية منتظمة من بياناتك{`\\n`}\n            • احفظ الملفات في مكان آمن{`\\n`}\n            • تأكد من صحة البيانات قبل الاستيراد{`\\n`}\n            • الاستيراد يحدّث البيانات الموجودة ولا يكررها إذا كانت بنفس المعرف{`\\n`}\n            • لا تشارك ملفات النسخ الاحتياطي مع جهات غير موثوقة\n          </Text>\n        </View>\n      </ScrollView>\n    </View>\n  );\n}\n\nconst styles = StyleSheet.create({\n  container: {\n    flex: 1,\n    backgroundColor: '#F9FAFB',\n  },\n  header: {\n    backgroundColor: '#FFFFFF',\n    paddingTop: 16,\n    paddingHorizontal: 20,\n    paddingBottom: 16,\n    flexDirection: 'row',\n    justifyContent: 'space-between',\n    alignItems: 'center',\n    borderBottomWidth: 1,\n    borderBottomColor: '#E5E7EB',\n  },\n  backButton: {\n    width: 40,\n    height: 40,\n    justifyContent: 'center',\n    alignItems: 'center',\n  },\n  headerTitle: {\n    fontSize: 20,\n    fontWeight: 'bold',\n    color: '#111827',\n  },\n  content: {\n    flex: 1,\n  },\n  infoCard: {\n    backgroundColor: '#FFFFFF',\n    margin: 16,\n    padding: 24,\n    borderRadius: 16,\n    alignItems: 'center',\n    shadowColor: '#000',\n    shadowOffset: { width: 0, height: 2 },\n    shadowOpacity: 0.05,\n    shadowRadius: 8,\n    elevation: 2,\n  },\n  infoTitle: {\n    fontSize: 20,\n    fontWeight: 'bold',\n    color: '#111827',\n    marginTop: 16,\n    marginBottom: 8,\n  },\n  infoText: {\n    fontSize: 14,\n    color: '#6B7280',\n    textAlign: 'center',\n    lineHeight: 22,\n  },\n  lastBackupContainer: {\n    flexDirection: 'row',\n    alignItems: 'center',\n    gap: 8,\n    marginTop: 16,\n    padding: 12,\n    backgroundColor: '#F9FAFB',\n    borderRadius: 8,\n  },\n  lastBackupText: {\n    fontSize: 14,\n    color: '#6B7280',\n  },\n  actionsSection: {\n    flexDirection: 'row',\n    gap: 12,\n    paddingHorizontal: 16,\n    marginBottom: 16,\n  },\n  actionCard: {\n    flex: 1,\n    padding: 24,\n    borderRadius: 16,\n    alignItems: 'center',\n    shadowColor: '#000',\n    shadowOffset: { width: 0, height: 2 },\n    shadowOpacity: 0.05,\n    shadowRadius: 8,\n    elevation: 2,\n  },\n  actionTitle: {\n    fontSize: 16,\n    fontWeight: 'bold',\n    color: '#111827',\n    marginTop: 12,\n    marginBottom: 8,\n    textAlign: 'center',\n  },\n  actionDescription: {\n    fontSize: 13,\n    color: '#6B7280',\n    textAlign: 'center',\n  },\n  warningCard: {\n    backgroundColor: '#FEF3C7',\n    margin: 16,\n    padding: 20,\n    borderRadius: 16,\n  },\n  warningTitle: {\n    fontSize: 16,\n    fontWeight: 'bold',\n    color: '#92400E',\n    marginBottom: 12,\n    textAlign: 'right',\n  },\n  warningText: {\n    fontSize: 14,\n    color: '#78350F',\n    lineHeight: 22,\n    textAlign: 'right',\n  },\n});\n";
  fs.writeFileSync(backupPath, nextBackup, 'utf8');
  report.push('✅ app/backup.tsx: تم تفعيل استيراد بيانات JSON واستبدال رسالة قيد التطوير.');
}

function patchReceiptPreview() {
  ensureFile(receiptPath);
  let src = fs.readFileSync(receiptPath, 'utf8');

  const helper = "\nfunction sanitizeReceiptPdfFileName(value: string | number | null | undefined): string {\n" +
    "  const safe = String(value || 'عميل')\n" +
    "    .trim()\n" +
    "    .replace(/[\\\\/:*?\\\"<>|#%{}~&]/g, '-')\n" +
    "    .replace(/\\s+/g, ' ')\n" +
    "    .slice(0, 70);\n" +
    "  return safe || 'عميل';\n" +
    "}\n\n" +
    "function buildReceiptPdfName(customerName: string | null | undefined, receiptNo: string | number | null | undefined): string {\n" +
    "  const safeCustomerName = sanitizeReceiptPdfFileName(customerName);\n" +
    "  const safeReceiptNo = sanitizeReceiptPdfFileName(receiptNo || new Date().toISOString().slice(0, 10));\n" +
    "  return `سند_${safeCustomerName}_${safeReceiptNo}.pdf`;\n" +
    "}\n";

  if (!src.includes('function buildReceiptPdfName')) {
    src = src.replace('export default function ReceiptPreviewScreen() {', `${helper}\nexport default function ReceiptPreviewScreen() {`);
    report.push('✅ app/receipt-preview.tsx: تم إضافة دالة تسمية ملف السند باسم العميل.');
  } else {
    report.push('ℹ️ app/receipt-preview.tsx: دالة تسمية ملف السند موجودة مسبقًا.');
  }

  const receiptNameOld = "const pdfName = `receipt_${movementData.receipt_number || movementData.movement_number}.pdf`;";
  const receiptNameNew = "const pdfName = buildReceiptPdfName(receiptData.customerName, movementData.receipt_number || movementData.movement_number);";
  if (src.includes(receiptNameOld)) {
    src = src.replace(receiptNameOld, receiptNameNew);
    report.push('✅ app/receipt-preview.tsx: تم تعديل اسم ملف PDF عند إنشاء السند.');
  } else if (src.includes(receiptNameNew)) {
    report.push('ℹ️ app/receipt-preview.tsx: اسم ملف PDF عند الإنشاء معدل مسبقًا.');
  } else {
    report.push('⚠️ app/receipt-preview.tsx: لم أجد سطر اسم PDF عند الإنشاء، راجعه يدويًا.');
  }

  const downloadNameOld = "const pdfName = `receipt_${movement.receipt_number || movement.movement_number}.pdf`;";
  const downloadNameNew = "const pdfName = buildReceiptPdfName((movement as any)?.customers?.name || (customerName as string) || 'عميل', movement.receipt_number || movement.movement_number);";
  if (src.includes(downloadNameOld)) {
    src = src.replace(downloadNameOld, downloadNameNew);
    report.push('✅ app/receipt-preview.tsx: تم تعديل اسم ملف PDF عند التنزيل.');
  } else if (src.includes(downloadNameNew)) {
    report.push('ℹ️ app/receipt-preview.tsx: اسم ملف PDF عند التنزيل معدل مسبقًا.');
  } else {
    report.push('⚠️ app/receipt-preview.tsx: لم أجد سطر اسم PDF عند التنزيل، راجعه يدويًا.');
  }

  fs.writeFileSync(receiptPath, src, 'utf8');
}

try {
  patchReceiptPreview();
  writeBackupFile();
  const reportPath = path.join(root, 'PATCH_RECEIPT_BACKUP_IMPORT_REPORT.txt');
  fs.writeFileSync(reportPath, report.join('\n'), 'utf8');
  console.log(report.join('\n'));
  console.log(`\nتم إنشاء التقرير: ${reportPath}`);
} catch (error) {
  console.error('فشل تطبيق التعديل:', error.message);
  process.exit(1);
}
